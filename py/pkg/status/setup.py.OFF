from setuptools import find_packages, setup
setup(
    name='status',
    packages=find_packages(include=['status']),
    version='0.1.0',
    description='Status gRPC library',
    author='Me',
    license='MIT',
)
