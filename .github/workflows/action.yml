name: ci

on:
  push:
    branches:
      - 'main'

env:
  IMAGE_NAME: mono-grpc-srvr
  PROJECT_ID: mono-346800

jobs:
  build-and-push-gcr:
    name: Build and Push to GCR
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - id: auth
        uses: google-github-actions/auth@v0
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v0

      - name: Authorize Docker push
        run: gcloud auth configure-docker

      - name: Build and Push Container
        run: |-
          docker build -f ./Dockerfile.grpc-srvr -t gcr.io/${{ env.PROJECT_ID }}/${{ env.IMAGE_NAME }}:${{  github.sha }} .
          docker tag gcr.io/${{ env.PROJECT_ID }}/${{ env.IMAGE_NAME }}:${{  github.sha }} gcr.io/${{ env.PROJECT_ID }}/${{ env.IMAGE_NAME }}:latest
          docker push gcr.io/${{ env.PROJECT_ID }}/${{ env.IMAGE_NAME }}:${{  github.sha }}    
          docker push gcr.io/${{ env.PROJECT_ID }}/${{ env.IMAGE_NAME }}:latest    

#      #        uses: google-github-actions/setup-gcloud@master
#      #        with:
#      #          service_account_key: ${{ secrets.SERVICE_ACCOUNT_KEY }}
#      #          project_id: ${{ env.PROJECT_ID }}
#      #          export_default_credentials: true
#      #      -
#
#      - name: Build Docker Image
#        run: docker build -f Dockerfile.grpc-srvr -t $IMAGE_NAME:latest .
#
#      - name: Configure Docker Client
#        run: |-
#          gcloud auth configure-docker --quiet
#
#      - name: Push Docker Image to GCR
#        env:
#          GIT_TAG: v0.10
#        run: |-
#          docker tag $IMAGE_NAME:latest gcr.io/$PROJECT_ID/$IMAGE_NAME:latest
#          docker tag gcr.io/$PROJECT_ID/$IMAGE_NAME:latest gcr.io/$PROJECT_ID/$IMAGE_NAME:$GIT_TAG
#          docker push gcr.io/$PROJECT_ID/$IMAGE_NAME:latest
#          docker push gcr.io/$PROJECT_ID/$IMAGE_NAME:$GIT_TAG
